## .gitignore File

`.gitignore` tells Git **which files or folders to ignore** so they are not tracked or committed.

### Why it’s used

- To avoid committing temporary files
- To keep secrets out of the repo
- To ignore build files and dependencies

### Common examples

```txt
node_modules/
.env
*.log
__pycache__/
```
